using System;

namespace ConsoleApplication1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Привет, Юзер!");
            Console.WriteLine("Как тебя зовут?");
            string user_Name = Convert.ToString(Console.ReadLine());
            Console.WriteLine("Привет, {0}, сколько тебе лет?", user_Name);
            int user_Age = Convert.ToInt16(Console.ReadLine());
            if (user_Age > 24)
           
            {
                Console.WriteLine("Ого, {0}, да ты старпёр!", user_Name, user_Age);
            }
            else 
            {
                Console.WriteLine("Фи, {0}, ну ты и мелюзга", user_Name);            
            }
            Console.WriteLine("Я умею считать. Загадай любое положительное целое число, меньшее 100: ");
            int count_User = Convert.ToInt16(Console.ReadLine());
            Console.WriteLine("Куб числа: {0}", Math.Pow(count_User,3));
            Console.ReadKey();

        }
    }
}
